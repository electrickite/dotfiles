#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

alias open=mimeo

if [ -f ~/.bash_common ]; then
  source ~/.bash_common
fi
